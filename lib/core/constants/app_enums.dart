// lib/core/utils/app_enums.dart

enum UserRole { hospital, ambulance, patient }

enum BedStatus { available, occupied, inTransit }

enum ReferralStatus { pending, confirmed, completed, cancelled }

enum AmbulanceStatus { active, inactive }

enum BookingType { nicuBed, ambulance }

enum BookingStatus { pending, confirmed, completed, cancelled }
